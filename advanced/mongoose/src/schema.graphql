type Member {
  name: String!
  age: Int!
}

input MemberInput {
  name: String!
  age: Int!
}

type Query {
  initProject: [Member!]!
  ageGreaterThan(age: Int!): [Member!]!
}

type Mutation {
  createMember(memberInput: MemberInput!): Member!
}

#type Subscription {
#}
